/**
 * К какому типу данных относится следующая переменная?
 * const c = "5";
 *
 * // string
 */

/**
 * Какой тип данных представляет переменная ниже?
 * const c = 91;
 * //number
 */

/**
 * Какая из строк обычно считается лучшей, строка 1 или строка 2?
 * let empty1 = undefined; //строка 1
 *
 * let empty2 = null; //строка 2
 *
 * //null
 */
/**
 * Какой результат после выполнения следующего кода будет выведен на экран?
 * let a = "Hello";
 *
 * a = "world";
 *
 * console.log(a);
 * //'world'
 */
/**
 * Каково значение a?
 * let a = "Hello";
 *
 * a = prompt("world");
 *
 * console.log(a);
 * //
 */
/**
 * Какое значение b будет выведено на экран?
 * let a = 5;
 *
 * let b = 70;
 *
 * let c = "5";
 *
 * b++;
 *
 * console.log(b);
 *
 * // 71
 */
/**
 * Каково значение result?
 * let result = 3 + 4 * 2 / 8;
 *
 * // 4
 */
/**
 * Каковы значения total и total2?
 * let firstNum = 5;
 * let secondNum = 10;
 *
 * firstNum++; // 6
 * secondNum--; // 9
 *
 * let total = ++firstNum + secondNum; // 7 + 9 = 16
 *
 * console.log(total);
 *
 * let total2 = 500 + 100 / 5 + total--; 500 + 20 + 16 = 536
 *
 * console.log(total2); // 536
 */
/**
 * Какой результат вы увидите на экране?
 * const a = 5;
 *
 * const b = 10;
 * console.log(a > 0 && b > 0); // true
 *
 * console.log(a == 5 && b == 4); // false
 *
 * console.log(true || false); // true
 *
 * console.log(a == 3 || b == 10); // true
 *
 * console.log(a == 3 || b == 7); //false
 */

